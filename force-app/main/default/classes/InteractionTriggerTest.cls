@isTest
public class InteractionTriggerTest {
    public static testMethod void linkRecordsTest() {
       //TestDataFactory.createTestData();
       // Inserting an Interaction
       // Quering for Interaction, Location
       //System.assertEquals(Interaction.Location__c, Location);
    }
}